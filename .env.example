# Copy to .env.local for local dev; set these in Vercel / Azure for deploys.

# Public site URL (used for metadata / canonical / sitemap)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# --- Reserved for later phases (not used by the Hero POC) ---
# Product database (catalog import)
# DATABASE_URL=

# CRM webhook / API (RFQ + enquiry routing)
# CRM_API_URL=
# CRM_API_KEY=
